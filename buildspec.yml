version: 0.2

phases:
  build:
    commands:
      - mkdir -p /build/lib
      - cp -R /usr/lib/swift/linux/*.so /build/lib
      - swift package update
      - swift build --product cci --configuration release
      - mv `swift build --product cci --configuration release --show-bin-path` /build/bin
      - mv Dockerfile /build/Dockerfile
artifacts:
  files:
    - 'bin/cci'
    - 'lib/**/*'
    - 'Dockerfile'
  base-directory: '/build/'
  discard-paths: no
